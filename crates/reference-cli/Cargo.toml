[package]
name = "reference-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
magnetron-core = { path = "../core" }
clap = { version = "4.4", features = ["derive"] }
diffsol = "0.14"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
